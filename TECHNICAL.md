# Техническая документация UDP-протокола лабораторной платы

Документ описывает формат UDP-пакетов, правила кодирования временных меток и результаты анализа эталонного захвата `test_TP_30_03_2026.pcapng`. Используется при разработке и тестировании симулятора `udp_lab_sim`.

---

## 1. Общие сведения

Лабораторная плата отправляет UDP-датаграммы с фиксированным размером поля данных. Приём и анализ выполняются через Wireshark.

| Уровень | Размер |
|---------|--------|
| Ethernet frame (Wireshark `frame.len`) | **1066 байт** |
| IP length | 1052 байт |
| UDP length | **1032 байт** (= 8 байт заголовок + 1024 данные) |
| UDP payload (поле данных) | **1024 байт** |

Разбивка кадра:

```
14 (Ethernet) + 20 (IP) + 8 (UDP) + 1024 (данные) = 1066 байт
```

---

## 2. Сетевые параметры

Параметры, подтверждённые анализом pcap (трафик только от платы):

| Параметр | Значение |
|----------|----------|
| IP источника (плата) | `192.168.10.10` |
| IP назначения | `192.168.10.2` |
| UDP порт источника | `4660` |
| UDP порт назначения | `4660` |
| IP TTL | `64` |
| UDP checksum | `0` (отключён) |
| IP ID | монотонно растёт (+1 на пакет) |

> **Примечание.** При отправке через обычный `socket.sendto()` IP- и UDP-заголовки формирует ОС. Порты, длина payload и checksum обычно совпадают с захватом. IP-адрес источника `192.168.10.10` появится в Wireshark только если он назначен на сетевой интерфейс отправляющей машины.

---

## 3. Каналы

В протоколе три канала:

| Канал | Назначение |
|-------|------------|
| **0** | Детектор фотонов #1 |
| **1** | Детектор фотонов #2 |
| **2** | Триггер |

В эталонном pcap плата одновременно передавала пакеты каналов 0 и 1 вперемешку (пары подряд). Для тестов симулятор может работать с одним каналом.

---

## 4. Структура UDP payload (1024 байта)

Все многобайтовые поля — **big-endian (network byte order)**.

| Смещение | Размер | Поле | Описание |
|----------|--------|------|----------|
| 0 | 1 байт | Номер канала | Младшие 4 бита — номер канала (0, 1, 2). Старшие 4 бита не интерпретируются (в тестах = 0) |
| 1 | 1 байт | Резерв | Всегда `0x00` |
| 2–3 | 2 байта | Счётчик | Счётчик отправленных UDP-пакетов (`uint16`). В pcap — **общий** для всех каналов |
| 4–7 | 4 байта | Временная метка #1 | Первое событие в потоке канала |
| 8–11 | 4 байта | Временная метка #2 | Следующее событие |
| … | … | … | … |
| 1020–1023 | 4 байта | Временная метка #255 | Последнее событие в пакете |

**Для приёмника `odmr` (packet_collector):** байты **4–1023** — непрерывный массив из **255** слов `uint32` BE (до 255 меток на UDP-пакет). Байты 0–3 — только служебные (канал, резерв, счётчик).

### 4.1. Байт 0 — номер канала

```
byte0 = channel & 0x0F
```

Флаг «100 мс метка в старших 4 битах» (hex `F`) в версии **21.04.2026 отсутствует** — используется только слово `0x00000000` в потоке меток.

Примеры:

| Канал | byte 0 (тесты) | byte 0 (в pcap) |
|-------|----------------|-----------------|
| 0 | `0x00` | `0xF0` (младшие 4 бита = 0, старшие не используются) |
| 1 | `0x01` | `0x01` |
| 2 | `0x02` | — |

### 4.2. Байты 2–3 — счётчик пакетов

16-битный счётчик в формате big-endian. В симуляторе стартует с **0**.

Пример из pcap (счётчик уже шёл с 600):

```
f0 00 02 58  →  канал 0, счётчик = 0x0258 = 600
01 00 02 59  →  канал 1, счётчик = 601
f0 00 02 5a  →  канал 0, счётчик = 602
```

---

## 5. Временная метка (байты 4–7 и 8–11)

Оба поля кодируются одинаковым 32-битным словом. Интерпретация:

### 5.1. Формат 32-битного слова

```
┌────────────────────────────────┬───┬─────────┐
│         bits [31:6]            │ 5 │ [4:0]   │
│   26 бит, грубая часть         │   │ 5 бит   │
│   × 5 нс                       │   │ × 185 пс│
└────────────────────────────────┴───┴─────────┘
```

**Формула декодирования:**

```
time_ns = (value[31:6] × 5 нс) + round(value[4:0] × 185 пс)
```

где `round` — округление к ближайшему целому, кратному **100 пс**.

**Примеры из документации:**

| coarse [31:6] | fine [4:0] | Расчёт | Итого |
|---------------|------------|--------|-------|
| 2 | 7 | (2×5 нс) + (7×185 пс) = 10 нс + 1295 пс | **11,3 нс** |
| 3 | 6 | (3×5 нс) + (6×185 пс) = 15 нс + 1110 пс | **16,1 нс** |

### 5.2. Каналы 0 и 1 (детекторы фотонов)

- **Бит 5** — разделитель, **всегда 0**, не участвует в расчёте времени.

```python
word = (coarse << 6) | fine    # bit 5 = 0
```

### 5.3. Канал 2 (триггер)

- **Бит 5** — флаг фронта сигнала:
  - переход `0 → 1` → в бит 5 записывается `1`
  - переход `1 → 0` → в бит 5 записывается `0`
- **Биты [4:0]** дублируют бит 5 и **не используются** в расчёте временной метки:
  - bit5 = 0 → fine = `0x00`
  - bit5 = 1 → fine = `0x1F`

```python
word = (coarse << 6) | (bit5 << 5) | (0x1F if bit5 else 0x00)
```

### 5.4. Специальные значения

| Значение | Интерпретация |
|----------|---------------|
| `0x00000000` | Маркер **+100 мс**: не является меткой времени; к последующим меткам канала прибавляется 100 мс |
| Метка = 0 (недопустимо) | Смещается на **+185 пс** (в пределах погрешности измерений) |

В потоках меток значение 0 как обычная метка времени **не встречается**.

---

## 6. Второй поток меток (байты 8–11)

В pcap оба поля (offset 4 и offset 8) кодируются по одному алгоритму, но несут **разные значения**. Для первого пакета канала 0:

| Поле | Декодированное время |
|------|---------------------|
| ts1 (bytes 4–7) | ~4 402 293 нс |
| ts2 (bytes 8–11) | ~5 402 299 нс |
| Смещение ts2 − ts1 | **~1 000 006 нс (~1 мс)** |

В симуляторе:

- `ts1` — основной поток, старт `1000 нс`, шаг `100 нс` на пакет
- `ts2` — независимый поток с начальным смещением `+1 000 000 нс`, тот же шаг

---

## 7. Анализ эталонного pcap

Файл: `test_TP_30_03_2026.pcapng` (рабочий стол).

Учитывается **только** трафик `ip.src == 192.168.10.10`. Остальное (TCF2, XML, link-local) игнорируется.

### 7.1. Статистика

| Параметр | Значение |
|----------|----------|
| Пакетов от платы | 151 |
| Размер frame | 1066 байт (все) |
| Размер payload | 1024 байт (все) |
| Канал 0 (`0xF0`) | 76 пакетов |
| Канал 1 (`0x01`) | 75 пакетов |

### 7.2. Порядок пакетов в захвате

Плата чередовала каналы 0 и 1 без паузы внутри пары:

```
ch0 → ch1 → (пауза ~255 µs) → ch0 → ch1 → ...
```

| Интервал | Значение |
|----------|----------|
| Между пакетами одной пары (ch0→ch1) | **0 µs** |
| Между парами | **~255 µs** |
| Между пакетами одного канала (ch0→ch0) | **~255 µs** |

### 7.3. Заголовки первых пакетов

```
#0  ch0  cnt=600  hdr=f0 00 02 58 | ts1=035bd28e | ts2=041f22d4
#1  ch1  cnt=601  hdr=01 00 02 59 | ts1=035bd091 | ts2=041f26d7
#2  ch0  cnt=602  hdr=f0 00 02 5a | ts1=2d552f88 | ts2=2e187fcd
#3  ch1  cnt=603  hdr=01 00 02 5b | ts1=2d552d8b | ts2=2e1883d1
```

### 7.4. Два уровня тайминга

| Уровень | Что измеряет | Типичное значение |
|---------|--------------|-------------------|
| **Временная метка в заголовке** | Внутреннее время события на плате | Шаг ~100 нс (параметр симулятора) |
| **Интервал UDP-кадров на сети** | Время между отправками по Ethernet | ~255 µs в pcap (параметр `--interval`) |

Это **разные величины**: метка в payload ≠ интервал между UDP-пакетами.

---

## 8. Режим симулятора (Synthesize)

Симулятор **не воспроизводит** pcap побайтно, а **генерирует** пакеты по правилам протокола:

1. Байты 0–3 — канал, резерв, счётчик (`uint16` BE)
2. Байты 4–1023 — **255 закодированных меток** (`--body-mode timestamps`, по умолчанию)
3. Интервал между событиями в пакете — `--event-step-ns`
4. Интервал между UDP-кадрами — `--timing` (fixed / random / list)
5. Legacy-режимы (`counter_fill`, `noise`, `counter`) — только для Wireshark; **не совместимы с odmr**

### 8.1. Соответствие коду

| Документация | Модуль |
|--------------|--------|
| Кодирование меток | `timestamp.py` |
| Сборка payload | `packet.py` |
| Неравномерные интервалы | `timing.py` |
| CLI и отправка | `udp_spammer.py` |

### 8.2. Интеграция с odmr

- Приёмник: Linux, raw Ethernet, кадр **1066** байт, `./cv_odmr eth0`
- Отправитель: `python udp_spammer.py --dst-host <IP_VM> --body-mode timestamps`
- Канал 2 + `analyze.py`: `--channel 2 --auto-toggle-edge`
- Проверка офлайн: `python verify_packets.py`

### 8.3. Расширение на 3 канала

Три канала можно имитировать тремя параллельными процессами. В pcap счётчик был **общим** — для полной идентичности потребуется координатор (`multiprocessing.Value`). В текущей версии каждый процесс ведёт свой счётчик с 0.

---

## 9. Wireshark

### Фильтры

Только трафик платы:

```
ip.src == 192.168.10.10 && udp.port == 4660 && frame.len == 1066
```

Проверка канала 0:

```
udp.payload[0] == 00
```

Проверка резервного байта:

```
udp.payload[1] == 00
```

Проверка размера payload:

```
udp.length == 1032
```

### Полезные колонки

- `udp.length` — должно быть 1032
- `data.len` или `udp.payload.length` — должно быть 1024

---

## 10. Ссылки на файлы проекта

```
udp_lab_sim/
├── TECHNICAL.md      ← этот документ
├── README.md         ← команды запуска
├── timestamp.py      ← кодирование временных меток
├── packet.py         ← сборка 1024-байтного payload
├── timing.py         ← генератор интервалов
├── config.py         ← значения по умолчанию
├── udp_spammer.py    ← основной скрипт
├── verify_packets.py ← офлайн-проверка заголовков
└── start_spammer.bat ← быстрый запуск
```

Эталонный захват (вне проекта): `C:\Users\dmitr\Desktop\test_TP_30_03_2026.pcapng`
